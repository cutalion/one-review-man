#!/usr/bin/env ruby

# frozen_string_literal: true

# Ensure package lib is on the load path
lib_dir = File.expand_path('../lib', __dir__)
$LOAD_PATH.unshift(lib_dir) unless $LOAD_PATH.include?(lib_dir)

require 'book/cli'

Book::CLI::Runner.start(ARGV)
